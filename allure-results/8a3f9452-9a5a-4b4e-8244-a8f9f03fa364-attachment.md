# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: leave.spec.ts >> Leave Module - Mandatory Suite @leave >> TC-LV-02-Apply for leave with end date before start date @regression
- Location: src\tests\leave.spec.ts:52:7

# Error details

```
Test timeout of 60000ms exceeded while running "beforeEach" hook.
```

# Test source

```ts
  1   | import { test, expect } from '../fixtures/base.fixture';
  2   | import { env } from '../utils/env';
  3   | import { LoginPage } from '../pages/auth/LoginPage';
  4   | import { PIMPage } from '../pages/PIMPage';
  5   | import { LeavePage, LeaveLocator } from '../pages/LeavePage'; 
  6   | import * as testData from '../utils/testData.json';
  7   | 
  8   | test.describe('Leave Module - Mandatory Suite @leave', () => {
  9   |   const testEmpFirstName = testData.employee.firstName;
  10  |   const testEmpLastName = testData.employee.lastName;
  11  |   const testFullName = `${testEmpFirstName} ${testEmpLastName}`;
  12  |   const testEmpId = testData.employee.employeeId;
  13  |   const today = new Date();
  14  |   const nextWeek = new Date();
  15  |   nextWeek.setDate(today.getDate() + 5);
  16  |   const validStartDate = today.toISOString().split('T')[0];
  17  |   const validEndDate = nextWeek.toISOString().split('T')[0];
  18  | 
  19  |   test.beforeAll(async ({ browser }) => {
  20  |     await test.step('Pre-condition: Clean up old data and add a fresh test employee in PIM', async () => {
  21  |       const context = await browser.newContext();
  22  |       const page = await context.newPage();
  23  |       const loginPage = new LoginPage(page);
  24  |       const pimPage = new PIMPage(page);
  25  |       await page.goto(`${env.baseUrl}/web/index.php/auth/login`);
  26  |       await loginPage.login(testData.admin.username, testData.admin.password);
  27  |       await pimPage.navigateToPIM();
  28  |       await pimPage.deleteEmployee(testEmpId).catch(() => {});
  29  |       await pimPage.addEmployee(testEmpFirstName, testEmpLastName, testEmpId);
  30  |       await context.close();
  31  |     });
  32  |   });
  33  | 
> 34  |   test.beforeEach(async ({ loginPage, leavePage }) => {
      |        ^ Test timeout of 60000ms exceeded while running "beforeEach" hook.
  35  |     await test.step('Pre-condition: Login to the system and navigate to the Leave Module', async () => {
  36  |       await loginPage.navigateTo('/web/index.php/auth/login');
  37  |       await loginPage.login(testData.admin.username, testData.admin.password);
  38  |       await leavePage.navigateToLeave();
  39  |     });
  40  |   });
  41  | 
  42  |   test('TC-LV-01-Apply for leave with valid date range @smoke', async ({ leavePage }) => {
  43  |     await test.step('Action: Fill in valid details and submit the leave request', async () => {
  44  |       await leavePage.applyLeaveAction(validStartDate, validEndDate);
  45  |     });
  46  |     await test.step('Verification: Leave request is submitted successfully (Toast message displayed)', async () => {
  47  |       await expect(leavePage.toast).toBeVisible({ timeout: 7000 });
  48  |       await expect(leavePage.toast).toContainText(/Successfully Saved|Success/i);
  49  |     });
  50  |   });
  51  | 
  52  |   test('TC-LV-02-Apply for leave with end date before start date @regression', async ({ leavePage }) => {
  53  |     await test.step('Action: Fill in a leave request where the end date precedes the start date', async () => {
  54  |       await leavePage.applyLeaveAction(testData.leave.startDate, testData.leave.invalidEndDate);
  55  |     });
  56  |     await test.step('Verification: System blocks submission and displays validation error under the date field', async () => {
  57  |       await expect(leavePage.dateErrorMessage.first()).toBeVisible();
  58  |       await expect(leavePage.dateErrorMessage.first()).toContainText(/To date should be after|cannot be before/i);
  59  |     });
  60  |   });
  61  | 
  62  |   test('TC-LV-04-Admin approves a pending leave request @regression', async ({ leavePage }) => {
  63  |     await test.step('Pre-condition: Create an isolated leave request in Pending status', async () => {
  64  |       await leavePage.applyLeaveAction(validStartDate, validEndDate);
  65  |       await expect(leavePage.toast).toContainText(/Successfully Saved|Success/i);
  66  |     });
  67  |     await test.step('Action: Search for the created employee leave request', async () => {
  68  |       await leavePage.searchLeaveRequestForEmployee(testFullName);
  69  |     });
  70  |     await test.step('Action: Click Approve on the first matching record in the table', async () => {
  71  |       const firstRow = leavePage.leaveTableRows.first();
  72  |       await expect(firstRow, 'No records found in the leave list table!').toBeVisible();
  73  |       const approveBtn = firstRow.getByRole('button', { name: LeaveLocator.approveRole });
  74  |       await expect(approveBtn, 'Approve button is missing from the record row!').toBeVisible();
  75  |       await approveBtn.click();
  76  |     });
  77  |     await test.step('Verification: System successfully updates the leave request status', async () => {
  78  |       await expect(leavePage.toast).toContainText(/Successfully Updated|Success/i);
  79  |     });
  80  |   });
  81  | 
  82  |   test('TC-LV-05-Admin rejects a pending leave request @regression', async ({ leavePage }) => {
  83  |     await test.step('Pre-condition: Create an isolated leave request specifically for the rejection case', async () => {
  84  |       await leavePage.applyLeaveAction(validStartDate, validEndDate);
  85  |       await expect(leavePage.toast).toContainText(/Successfully Saved|Success/i);
  86  |     });
  87  |     await test.step('Action: Search for the created employee leave request', async () => {
  88  |       await leavePage.searchLeaveRequestForEmployee(testFullName);
  89  |     });
  90  |     await test.step('Action: Click Reject on the first matching record in the table', async () => {
  91  |       const firstRow = leavePage.leaveTableRows.first();
  92  |       await expect(firstRow, 'No records found in the leave list table!').toBeVisible();
  93  |       const rejectBtn = firstRow.getByRole('button', { name: LeaveLocator.rejectRole });
  94  |       await expect(rejectBtn, 'Reject button is missing from the record row!').toBeVisible();
  95  |       await rejectBtn.click();
  96  |     });
  97  |     await test.step('Verification: System successfully records the rejection status', async () => {
  98  |       await expect(leavePage.toast).toContainText(/Successfully Updated|Success/i);
  99  |     });
  100 |   });
  101 | });
```